# 开发手册

## 开发流程

开始任何工作前，先更新技术方案，然后根据技术方案的优先级生成新的todo，接着按照todo开展工作。

## 技术分歧

如果出现技术方案中没有详细解释的边界情况，可以参考这个项目 `/Users/sander/coding/cesium-terrain-builder`，使用了 GDAL 默认行为和默认参数的地方，请参考 `/Users/sander/coding/cesium-terrain-builder`。

如果当前已经实现的内容与技术方案有区别，要以ctb原版的实现为准对技术文档进行更新。不要擅自添加原版没有的内容、算法和接口，也不要用rust的语法糖或其他特性替代原版的技术路线，必须遵照原版的实现方式进行重写。

你可以持续自行迭代开发这个项目，但是在开发过程中你遇到技术方案无法正确落地的时候，你必须停下来，向我询问，不得自行更改技术方案中已有的设计和决策。

## 实施要点

实施过程中，未经我直接要求，不得修改代码和配置文件。

要有领域设计优先原则，先生成或更新计划、接口、测试策略，再引入副作用。

## 注意事项

没有经过授权，不能手动编辑cargo.toml，新增或移除依赖等操作时必须要通过cargo cli进行。如果cargo cli无法表达变更，必须立刻停下来向我询问。执行cargo命令的时候必须禁用沙盒。

生产代码中，不允许使用可能panic的unwarp。如果穷尽分支已经证明，不变量可使用expect且消息必须说明该不变量。
